{
  "name": "exotic-pets-pdf-service",
  "version": "1.0.0",
  "description": "Simple PDF generation service for existing exotic pets orders",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "docker:build": "docker build -t pdf-service .",
    "docker:run": "docker run -p 8080:8080 pdf-service"
  },
  "dependencies": {
    "express": "^4.18.2",
    "mongodb": "^6.20.0",
    "cors": "^2.8.5",
    "helmet": "^7.1.0",
    "morgan": "^1.10.0",
    "dotenv": "^16.3.1",
    "pdfkit": "^0.14.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "keywords": [
    "pdf",
    "invoice",
    "orders",
    "microservice"
  ]
}